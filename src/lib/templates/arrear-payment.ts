import { SHARED_DOC_CSS, HEADER_HTML, FOOTER_HTML, PAGINATION_SCRIPT } from './shared-css'
export function arrearPaymentHTML(data: Record<string, any>): string {
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/><title>Arrear Payment - ${data.name||'Employee'}</title><style>${SHARED_DOC_CSS}</style></head><body><div class="page page-first">${HEADER_HTML()}<div class="pg-body" contenteditable="true" spellcheck="true"><div class="cert-title">ARREAR PAYMENT LETTER</div><div class="ref-date"><span>Ref: ${data.ref_code||''}</span><span>Date: ${data.date_fmt||''}</span></div><div class="salutation">To,</div><div class="emp-name-block">${data.name||''}</div><div class="detail-row"><span class="label">Department:</span><span class="value">${data.department||''}</span></div><div class="cert-body"><strong>Sub: Arrear Payment Notification</strong></div><div class="cert-body">Dear ${data.name ? data.name.split(' ')[0] : 'Sir/Madam'},</div><div class="cert-body">This is to notify you that an arrear payment of <strong>BDT ${data.arrear_amount||'0'}/-</strong> has been approved for you, pertaining to the period <strong>${data.arrear_period||''}</strong>.</div><div class="cert-body">The above amount represents the difference arising from the salary revision effective <strong>${data.revision_date_fmt||''}</strong> and will be credited to your bank account along with the salary for ${data.pay_month||'the current month'}.</div><div class="cert-body">For any clarification, please contact the accounts department.</div><div class="sig-section"><div class="sig-block"><div class="sig-name">${data.proprietor_name||'Authorized Signatory'}</div><div class="sig-title">${data.proprietor_designation||'Authorized Signatory'}<br>${data.company_name||'Beyond Headlines'}</div></div></div></div>${FOOTER_HTML()}</div>${PAGINATION_SCRIPT}</body></html>`
}
