import { SHARED_DOC_CSS, HEADER_HTML, FOOTER_HTML, PAGINATION_SCRIPT } from './shared-css'
export function bankIntroLetterHTML(data: Record<string, any>): string {
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/><title>Bank Intro Letter - ${data.name||'Employee'}</title><style>${SHARED_DOC_CSS}</style></head><body><div class="page page-first">${HEADER_HTML()}<div class="pg-body" contenteditable="true" spellcheck="true"><div class="cert-title">BANK INTRODUCTION LETTER</div><div class="ref-date"><span>Ref: ${data.ref_code||''}</span><span>Date: ${data.date_fmt||''}</span></div><div class="salutation">To,</div><div class="cert-body">The Branch Manager<br>${data.bank_name||''}<br>${data.bank_branch||''}</div><div class="cert-body"><strong>Sub: Introduction Letter - ${data.name||''}</strong></div><div class="cert-body">Dear Sir/Madam,</div><div class="cert-body">This is to introduce <strong>${data.name||''}</strong>, Employee ID <strong>${data.employee_id||''}</strong>, who is employed at ${data.company_name||'Beyond Headlines'} as <strong>${data.designation||''}</strong> in the <strong>${data.department||''}</strong> department since <strong>${data.joining_date_fmt||''}</strong>.</div><div class="cert-body">${data.pronoun||'He'} is maintaining a good record with the organization and is considered trustworthy. We recommend ${data.pronoun||'him'} for opening a salary account / availing banking facilities with your esteemed bank.</div><div class="cert-body">Any assistance extended to ${data.pronoun||'him'} will be highly appreciated.</div><div class="sig-section"><div class="sig-block"><div class="sig-name">${data.proprietor_name||'Authorized Signatory'}</div><div class="sig-title">${data.proprietor_designation||'Authorized Signatory'}<br>${data.company_name||'Beyond Headlines'}</div></div></div></div>${FOOTER_HTML()}</div>${PAGINATION_SCRIPT}</body></html>`
}
