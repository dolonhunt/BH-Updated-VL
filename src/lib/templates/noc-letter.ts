import { SHARED_DOC_CSS, HEADER_HTML, FOOTER_HTML, PAGINATION_SCRIPT } from './shared-css'
export function nocLetterHTML(data: Record<string, any>): string {
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/><title>NOC Letter - ${data.name||'Employee'}</title><style>${SHARED_DOC_CSS}</style></head><body><div class="page page-first">${HEADER_HTML()}<div class="pg-body" contenteditable="true" spellcheck="true"><div class="cert-title">NO OBJECTION CERTIFICATE</div><div class="ref-date"><span>Ref: ${data.ref_code||''}</span><span>Date: ${data.date_fmt||''}</span></div><div class="salutation">To Whom It May Concern,</div><div class="cert-body">This is to certify that <strong>${data.name||''}</strong>, son/daughter of <strong>${data.father_name||''}</strong>, holding Employee ID <strong>${data.employee_id||''}</strong>, has been employed at ${data.company_name||'Beyond Headlines'} as <strong>${data.designation||''}</strong> from <strong>${data.joining_date_fmt||''}</strong> to <strong>${data.leaving_date_fmt||''}</strong>.</div><div class="cert-body">${data.company_name||'The company'} has no objection to ${data.pronoun||'him'}/${data.possessive||'his'} pursuing higher education / obtaining a passport / traveling abroad / applying for a visa, as the case may be.</div><div class="cert-body">This certificate is issued upon the request of the employee for ${data.noc_purpose||'official purposes'} and is valid for ${data.noc_validity||'one year'} from the date of issue.</div><div class="sig-section"><div class="sig-block"><div class="sig-name">${data.proprietor_name||'Authorized Signatory'}</div><div class="sig-title">${data.proprietor_designation||'Authorized Signatory'}<br>${data.company_name||'Beyond Headlines'}</div></div></div></div>${FOOTER_HTML()}</div>${PAGINATION_SCRIPT}</body></html>`
}
